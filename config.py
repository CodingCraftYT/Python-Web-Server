# Define a configuration class for your Flask application
class Config:
    # Set the secret key for the application (Change this to a secure, random key)
    SECRET_KEY = 'your_secret_key'  # Change this to a random, long, and unique string
